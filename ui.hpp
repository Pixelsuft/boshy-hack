#pragma once

extern bool win_shown;
extern bool no_cursor_kill;
extern bool skip_msg_box;
extern bool new_rng_custom_seed;
extern int new_rng_seed;
extern bool new_rng_custom;
extern int new_rng;
extern bool god_mode;

void ui_render();
